import { defineComponent, ref } from "vue";


export const Navbar = defineComponent({
    setup: (props, context) => {
        return () => (
            <div>
                
            </div>
        );
    }
})